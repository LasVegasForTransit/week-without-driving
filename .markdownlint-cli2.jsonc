{
  // Documentation is checked like code: every relative link and anchor must
  // resolve, and prose wraps at the same 100 columns Prettier uses.
  "customRules": ["markdownlint-rule-relative-links"],
  "config": {
    "default": true,
    "MD013": { "line_length": 100, "code_blocks": false, "tables": false, "headings": false },
    "MD033": false,
    "relative-links": true,
  },
  "globs": ["**/*.md"],
  "ignores": [
    // The vendored standard is integrity-checked as an exact upstream snapshot.
    ".lvbt/web-platform",
    // Playwright output.
    "**/test-results",
    "**/playwright-report",
    ".claude/worktrees",
    "node_modules",
    "**/node_modules",
    "dist",
    "**/dist",
    ".turbo",
    "docs/superpowers",
    "CHANGELOG.md",
  ],
}
